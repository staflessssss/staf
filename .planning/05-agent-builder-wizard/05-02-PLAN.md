---
phase: 05-agent-builder-wizard
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/components/stafless/agent-builder.tsx
  - src/app/admin/tenants/[tenantId]/agents/new/page.tsx
  - src/app/admin/tenants/[tenantId]/agents/[agentId]/edit/page.tsx
autonomous: true
requirements:
  - AGNT-01
  - AGNT-02
  - AGNT-03
  - AGNT-04
  - AGNT-06
user_setup: []
must_haves:
  truths:
    - "Operator can move through the five-step wizard with real create/edit state."
    - "Knowledge and tool blocks can be added, edited, removed, and reordered without fallback demo content."
    - "Only valid tenant channels can be selected, and unavailable channels are explained."
  artifacts:
    - path: "src/components/stafless/agent-builder.tsx"
      provides: "Controlled Phase 5 wizard UI"
      min_lines: 200
    - path: "src/app/admin/tenants/[tenantId]/agents/new/page.tsx"
      provides: "Create flow wired to real builder actions"
    - path: "src/app/admin/tenants/[tenantId]/agents/[agentId]/edit/page.tsx"
      provides: "Edit flow wired to real builder actions"
  key_links:
    - from: "src/components/stafless/agent-builder.tsx"
      to: "src/app/api/admin/tenants/[tenantId]/agents/route.ts"
      via: "create submission"
    - from: "src/components/stafless/agent-builder.tsx"
      to: "src/app/api/admin/tenants/[tenantId]/agents/[agentId]/route.ts"
      via: "edit submission"
---

<objective>
Convert the Phase 4 shell into a real wizard UI for create and edit flows.

Purpose: operators already have the visual frame, but they still cannot actually build an agent from the admin surface.
Output: controlled wizard state, real navigation, block CRUD/reorder behavior, and working create/edit submission UX.
</objective>

<execution_context>
@C:/Users/Viktor/Projects/staf/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Viktor/Projects/staf/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/04-ui-foundation-agent-builder-ux/04-UI-SPEC.md
@.planning/05-agent-builder-wizard/05-CONTEXT.md
@.planning/05-agent-builder-wizard/05-RESEARCH.md
@src/components/stafless/agent-builder.tsx
@src/components/stafless/foundation.tsx
@src/app/admin/tenants/[tenantId]/agents/new/page.tsx
@src/app/admin/tenants/[tenantId]/agents/[agentId]/edit/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace mock builder state with controlled wizard flow</name>
  <files>src/components/stafless/agent-builder.tsx</files>
  <read_first>src/components/stafless/agent-builder.tsx, src/components/stafless/foundation.tsx, .planning/04-ui-foundation-agent-builder-ux/04-UI-SPEC.md</read_first>
  <action>Refactor the builder component so it manages explicit step state, field state, validation state, and create-vs-edit behavior instead of relying on `defaultValue`, hardcoded current-step indices, or dead buttons. Preserve the approved Phase 4 visual contract while making back/next progression, disabled states, and error guidance real.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/components/stafless/agent-builder.tsx` no longer relies on static `currentStep` values based only on mode
    - primary and secondary builder actions have real handlers rather than placeholder buttons only
    - validation messaging follows the Phase 4 operational copy style
  </acceptance_criteria>
  <done>The wizard behaves like a real multi-step editor instead of a static mockup.</done>
</task>

<task type="auto">
  <name>Task 2: Implement knowledge/tool block CRUD and channel selection UX</name>
  <files>src/components/stafless/agent-builder.tsx</files>
  <read_first>src/components/stafless/agent-builder.tsx, prisma/schema.prisma, .planning/05-agent-builder-wizard/05-CONTEXT.md</read_first>
  <action>Replace `sampleKnowledge` and `sampleTools` fallback behavior with real draft blocks that can be added, edited, removed, and reordered. Add a real channel selector that distinguishes available and unavailable connected channels, explains assignment conflicts, and stays aligned with AGNT-06. Keep the UI editorial and card-based, not spreadsheet-like.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - the builder does not inject hardcoded sample knowledge/tools for normal create flow
    - knowledge blocks support add, edit, remove, and reorder behavior
    - tool blocks support add, edit, remove, and reorder behavior
    - channel selection shows unavailable channels with an explanation instead of silently hiding the rule
  </acceptance_criteria>
  <done>Operators can fully shape the agent body and channel choice from the wizard UI.</done>
</task>

<task type="auto">
  <name>Task 3: Wire create/edit pages to the real wizard submission path</name>
  <files>src/app/admin/tenants/[tenantId]/agents/new/page.tsx, src/app/admin/tenants/[tenantId]/agents/[agentId]/edit/page.tsx</files>
  <read_first>src/app/admin/tenants/[tenantId]/agents/new/page.tsx, src/app/admin/tenants/[tenantId]/agents/[agentId]/edit/page.tsx, src/components/stafless/agent-builder.tsx</read_first>
  <action>Update the create and edit pages so their header actions, success/error states, and navigation all reflect the real wizard lifecycle. Remove copy that still frames the builder as Phase 4-only UI scaffolding, and replace it with draft/save/review language appropriate for a real Phase 5 flow.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - create page description no longer says persistence lands in Phase 5 later
    - create and edit pages both pass the data needed for real submission and result handling
    - operator can save a draft and continue to review from the UI flow
  </acceptance_criteria>
  <done>Create and edit routes behave like working entry points into the Phase 5 wizard.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint succeeds
- [ ] create flow can capture basics, channel, knowledge, and tools without dead-end buttons
- [ ] edit flow preserves and updates persisted builder state
</verification>

<success_criteria>

- All tasks completed
- Wizard UI satisfies AGNT-01 through AGNT-04 from the admin surface
- AGNT-06 is visible and understandable in the operator UX
- No sample/demo-only builder content remains in the normal flow
  </success_criteria>

<output>
After completion, create `.planning/05-agent-builder-wizard/05-02-SUMMARY.md`
</output>
