---
phase: 05-agent-builder-wizard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - package.json
  - src/app/api/admin/tenants/[tenantId]/agents/route.ts
  - src/app/api/admin/tenants/[tenantId]/agents/[agentId]/route.ts
autonomous: true
requirements:
  - AGNT-01
  - AGNT-02
  - AGNT-03
  - AGNT-04
  - AGNT-06
user_setup: []
must_haves:
  truths:
    - "Admin can persist an agent draft with name, persona, tone, optional language preference, and a valid tenant channel."
    - "The same connected channel cannot be assigned to two agents."
    - "Knowledge blocks and tool definitions persist in order and remain tenant-scoped."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Phase 5 agent schema including optional language preference and channel-safety constraint"
      contains: "channelId"
    - path: "src/app/api/admin/tenants/[tenantId]/agents/route.ts"
      provides: "Tenant-scoped agent list/create route"
      exports: ["GET", "POST"]
    - path: "src/app/api/admin/tenants/[tenantId]/agents/[agentId]/route.ts"
      provides: "Tenant-scoped agent detail/update route"
      exports: ["GET", "PATCH"]
  key_links:
    - from: "src/app/api/admin/tenants/[tenantId]/agents/route.ts"
      to: "prisma.agent"
      via: "tenant-scoped create/list queries"
      pattern: "db\\.agent\\."
    - from: "src/app/api/admin/tenants/[tenantId]/agents/[agentId]/route.ts"
      to: "prisma.feature"
      via: "ordered knowledge/tool persistence"
      pattern: "features|FeatureType"
---

<objective>
Build the tenant-safe backend contract for the Phase 5 wizard.

Purpose: the UI shell already exists, but Phase 5 cannot become real until agent drafts, channel assignment, and free-form knowledge/tool data can be persisted and validated.
Output: schema updates, request validation, and real admin create/update/list/detail endpoints for agents.
</objective>

<execution_context>
@C:/Users/Viktor/Projects/staf/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Viktor/Projects/staf/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/05-agent-builder-wizard/05-CONTEXT.md
@.planning/05-agent-builder-wizard/05-RESEARCH.md
@prisma/schema.prisma
@src/app/api/admin/tenants/[tenantId]/agents/route.ts
@src/app/api/admin/tenants/[tenantId]/agents/[agentId]/route.ts
@src/app/admin/clients/[clientId]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the agent schema and validation contract for Phase 5</name>
  <files>prisma/schema.prisma, package.json</files>
  <read_first>prisma/schema.prisma, package.json, .planning/REQUIREMENTS.md, .planning/05-agent-builder-wizard/05-RESEARCH.md</read_first>
  <action>Add an optional multilingual-friendly `languagePreference` field to `Agent` only if persistence is needed, add a storage-level uniqueness constraint that prevents one `channelId` from belonging to multiple agents, and define or wire the minimal validation/test tooling needed for this phase's backend rules. Keep the schema tenant-safe and compatible with later Phase 6 deploy fields rather than introducing a second draft model.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` contains either a persisted optional language-preference field on `Agent` or an explicit multilingual-first choice with no hard single-language requirement
    - `prisma/schema.prisma` contains a uniqueness guard for channel assignment
    - `package.json` includes a stable test command or supporting script for Phase 5 validation
  </acceptance_criteria>
  <done>Schema and validation foundation cover AGNT-01 and AGNT-06 without changing the product boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Implement tenant-scoped create and update APIs for builder output</name>
  <files>src/app/api/admin/tenants/[tenantId]/agents/route.ts, src/app/api/admin/tenants/[tenantId]/agents/[agentId]/route.ts</files>
  <read_first>src/app/api/admin/tenants/[tenantId]/agents/route.ts, src/app/api/admin/tenants/[tenantId]/agents/[agentId]/route.ts, prisma/schema.prisma, src/app/admin/clients/[clientId]/page.tsx</read_first>
  <action>Replace the placeholder routes with real `GET`/`POST`/`PATCH` handlers that validate admin access, load the tenant, validate channel availability, reject cross-tenant integration references, and persist ordered knowledge/tool structures using the existing `Agent` + `Feature` + `Step` model. Use transactional writes so partial feature-step updates cannot leave the draft in an inconsistent state.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/app/api/admin/tenants/[tenantId]/agents/route.ts` no longer returns an empty `items: []` stub for all cases
    - `src/app/api/admin/tenants/[tenantId]/agents/route.ts` no longer returns `501` for create
    - `src/app/api/admin/tenants/[tenantId]/agents/[agentId]/route.ts` no longer returns `501` for update
    - write-path validation explicitly checks channel uniqueness and tenant ownership for integrations
  </acceptance_criteria>
  <done>Admin routes can persist builder drafts safely for create and edit flows.</done>
</task>

<task type="auto">
  <name>Task 3: Add targeted automated checks for builder persistence rules</name>
  <files>package.json</files>
  <read_first>.planning/05-agent-builder-wizard/05-VALIDATION.md, package.json, src/app/api/admin/tenants/[tenantId]/agents/route.ts, src/app/api/admin/tenants/[tenantId]/agents/[agentId]/route.ts</read_first>
  <action>Add focused automated coverage for the rules that are easy to regress: multilingual basics handling, duplicate channel rejection, ordered feature persistence, and cross-tenant integration blocking. Keep the test footprint tight and specific to Phase 5 rather than introducing a large generalized testing migration.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - the repo has an executable targeted test path for the Phase 5 backend rules
    - at least one automated check covers duplicate channel rejection
    - at least one automated check covers cross-tenant integration rejection
  </acceptance_criteria>
  <done>Phase 5 backend rules have fast regression coverage beyond manual clicking.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint succeeds
- [ ] backend validation rejects duplicate channel assignment
- [ ] backend validation rejects tool bindings to another tenant's integration
</verification>

<success_criteria>

- All tasks completed
- Admin agent APIs support real draft persistence
- AGNT-01, AGNT-02, AGNT-03, AGNT-04, and AGNT-06 have backend support
- No channel or integration safety rule remains UI-only
  </success_criteria>

<output>
After completion, create `.planning/05-agent-builder-wizard/05-01-SUMMARY.md`
</output>
