---
phase: 05-agent-builder-wizard
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - src/lib/prompt-builder.ts
  - src/components/stafless/agent-builder.tsx
  - src/app/api/agent/invoke/route.ts
  - src/lib/ai-runtime.ts
autonomous: true
requirements:
  - AGNT-05
user_setup: []
must_haves:
  truths:
    - "Prompt preview reflects the current wizard configuration, including ordered knowledge and tool definitions."
    - "Operator can run a pre-deploy sandbox test from the review step."
    - "Review step clearly surfaces what is missing before the agent is ready for deployment in Phase 6."
  artifacts:
    - path: "src/lib/prompt-builder.ts"
      provides: "Prompt assembly logic for preview and test mode"
      contains: "knowledge"
    - path: "src/components/stafless/agent-builder.tsx"
      provides: "Review, readiness, and sandbox UI wired to live draft state"
    - path: "src/app/api/agent/invoke/route.ts"
      provides: "Draft-safe sandbox invocation path or equivalent review test entry point"
      exports: ["POST"]
  key_links:
    - from: "src/components/stafless/agent-builder.tsx"
      to: "src/lib/prompt-builder.ts"
      via: "review preview rendering"
    - from: "src/components/stafless/agent-builder.tsx"
      to: "src/app/api/agent/invoke/route.ts"
      via: "sandbox test request"
---

<objective>
Complete the review layer of the wizard with accurate prompt preview and draft-safe test mode.

Purpose: Phase 5 is only complete when the operator can review what the agent will do and test it before Phase 6 deployment work begins.
Output: a real prompt preview, readiness checklist, and sandbox test flow integrated into the review step.
</objective>

<execution_context>
@C:/Users/Viktor/Projects/staf/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Viktor/Projects/staf/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/04-ui-foundation-agent-builder-ux/04-UI-SPEC.md
@.planning/05-agent-builder-wizard/05-CONTEXT.md
@.planning/05-agent-builder-wizard/05-RESEARCH.md
@src/lib/prompt-builder.ts
@src/components/stafless/agent-builder.tsx
@src/app/api/agent/invoke/route.ts
@src/lib/ai-runtime.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build faithful prompt preview generation from wizard state</name>
  <files>src/lib/prompt-builder.ts, src/components/stafless/agent-builder.tsx</files>
  <read_first>src/lib/prompt-builder.ts, src/components/stafless/agent-builder.tsx, .planning/05-agent-builder-wizard/05-CONTEXT.md</read_first>
  <action>Replace the placeholder prompt builder with a structured generator that includes agent identity, tone, optional language preference, multilingual behavior guidance, channel context, ordered knowledge blocks, and tool summaries. Use the same source shape for both saved agents and in-progress drafts so preview output cannot drift from what the operator is editing.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/lib/prompt-builder.ts` no longer returns only `persona` and `tone`
    - prompt preview output includes multilingual guidance plus knowledge/tool-derived sections
    - review UI uses the same prompt builder for current draft state
  </acceptance_criteria>
  <done>Prompt preview becomes a meaningful operator review tool instead of a placeholder string.</done>
</task>

<task type="auto">
  <name>Task 2: Implement a draft-safe sandbox test path</name>
  <files>src/app/api/agent/invoke/route.ts, src/lib/ai-runtime.ts</files>
  <read_first>src/app/api/agent/invoke/route.ts, src/lib/ai-runtime.ts, src/lib/prompt-builder.ts, .planning/05-agent-builder-wizard/05-RESEARCH.md</read_first>
  <action>Create a pre-deploy test flow that accepts a sample message from the review step, builds the current draft prompt, executes a sandbox response path, and returns the result without activating live channel delivery. Keep the route explicitly tenant-scoped and safe for admin review. If full runtime behavior is not yet available, implement the minimal draft-safe invocation needed for AGNT-05 without pretending deploy is complete.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - there is a non-501 POST path for review-step sandbox testing
    - the sandbox path is clearly separate from live deploy activation
    - sandbox responses are based on current builder state rather than hardcoded copy
  </acceptance_criteria>
  <done>Operators can test agent behavior before deployment with a real response path.</done>
</task>

<task type="auto">
  <name>Task 3: Back the review checklist and CTAs with real readiness logic</name>
  <files>src/components/stafless/agent-builder.tsx</files>
  <read_first>src/components/stafless/agent-builder.tsx, .planning/04-ui-foundation-agent-builder-ux/04-UI-SPEC.md, .planning/REQUIREMENTS.md</read_first>
  <action>Replace the static checklist and review CTA logic with real readiness rules that surface missing basics, missing channel selection, empty knowledge/tools where required, and sandbox status. Keep deploy language forward-looking for Phase 6, but ensure the operator can clearly tell whether the draft is ready to hand off into deployment work.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - review checklist derives from actual builder validation state
    - review CTAs no longer rely on placeholder behavior
    - the sandbox panel no longer renders only a fixed canned conversation
  </acceptance_criteria>
  <done>The review step functions as a genuine pre-deploy checkpoint for Phase 5.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint succeeds
- [ ] npm run build succeeds
- [ ] prompt preview changes when knowledge, tools, or language preference change
- [ ] sandbox test returns a real draft-backed response path
</verification>

<success_criteria>

- All tasks completed
- AGNT-05 is satisfied with real preview and test behavior
- Review step clearly prepares the handoff to Phase 6 without absorbing Phase 6 scope
  </success_criteria>

<output>
After completion, create `.planning/05-agent-builder-wizard/05-03-SUMMARY.md`
</output>
